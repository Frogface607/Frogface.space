feat: Studio v2 — individual agent pages with chat, tasks, activity log

- /studio: clickable card grid with hover effects and status indicators
- /studio/[id]: full agent dashboard with:
  - Profile header with status toggle
  - Chat interface with simulated per-agent responses
  - Task list with add/complete functionality
  - Activity log
  - System prompt and model info
- Shared agent data extracted to src/lib/agents.ts