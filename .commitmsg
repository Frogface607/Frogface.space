Mobile responsive + real AI chat via OpenRouter

- Fix all pages for mobile: responsive grids, compact padding, flex-wrap
- HQ: 2-col stats on mobile, stacked quests/log sections
- Player: vertical layout on small screens, compact avatar + credo
- Projects: single column on mobile, wrapped metrics
- Studio: single column agent cards, stacked chat/sidebar on mobile
- Quests: tighter padding on chain headers
- Command: adjusted viewport height for mobile top bar
- Add /api/chat streaming proxy to OpenRouter API
- Agent chat pages now use real AI with streaming responses
- Command center (Moltbot) uses real AI with streaming
- Fallback to simulated responses when API is unavailable
