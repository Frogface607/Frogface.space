Better error visibility for AI chat + redeploy trigger

- Add GET /api/chat health check (shows if API key is configured)
- Show actual error messages in chat instead of silent fallback
- Better error handling: parse OpenRouter error responses
- Wrap fetch in try-catch for network failures
- Trigger Vercel redeploy to pick up OPENROUTER_API_KEY env var
