import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Studio — Frogface" };

export default function StudioPage() {
  return (
    <PageStub
      num="05"
      kicker="studio / sales hub"
      title="нужна такая же система?"
      hint="Edison Toolkit как продукт: 11 модулей, 3 пакета ($3K / $5K / $7.5–10K). Сразу подключим из edison-toolkit-catalog.md после первого WOW-апрува главной."
    />
  );
}
